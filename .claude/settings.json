{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q \"^/Users/seui/Documents/vibe_coding/english\" && cd /Users/seui/Documents/vibe_coding/english && git add -A && git diff --cached --quiet || git commit -m \"auto: update code\" && git push origin main; } 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
